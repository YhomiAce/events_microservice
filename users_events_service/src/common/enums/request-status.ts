export enum RequestStatus {
    PENDING = "Pending",
    ACCEPTED = "Accepted",
    REJECTED = "Rejected",
}

export enum RequestDecisionStatus {
    ACCEPTED = "Accepted",
    REJECTED = "Rejected",
}